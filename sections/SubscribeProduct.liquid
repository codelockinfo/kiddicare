<div class="subscrimg" style ="background-image: url('https://cdn.shopify.com/s/files/1/0431/8039/2600/files/DSC06690_3_1.png?v=1664195091')">
  <h2 class="subkatitle">subscribe and save </h2>
</div>
<div class="subscribetab">
  <div class="subscribeciontain">
    <div class="subscribedaa">
      <div class="choicetitle">
        <h3 class="chocetitle">{{ section.settings.komtitle }}</h3>
      </div>
      <div class="sudalldata">
     <div class="subscribe_flex">
{% for block in section.blocks %}
  <div class="subscribleflex1">
    
   {% assign product = all_products[block.settings.product] %}
   {% for image in product.images %}
        {% if forloop.first == true %}
        <h3 class="prodtitle">{{product.title}}</h3>      
        <img src="{{ product.images[0] | img_url:'master' }}" class="customproduce"> 

 
      {% for option in product.options_with_values %}
<div class="customslide">
                     <label>
                          {{ option.name }}
                        </label>
                        <select class="customslider">
                          {% for value in option.values %}
                            <option value="{{ value | escape }}"{% if option.selected_value == value %} selected="selected"{% endif %}>{{ value }}</option>
                          {% endfor %}
                        </select>
</div>
          {% endfor %}
   
    {% endif %}
  
   {% endfor %}
 </div>
{% endfor %}
     </div>
      </div>
    </div>
  </div>
</div>
  <style>
      .subscribleflex1 {
    flex: 0 0 24%;
    max-width: 24%;
}
  .subscribe_flex {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    text-align: center;
        align-items: flex-end;
}
    .subscrimg {
    height: 50vh;
    background-size: cover;
    background-repeat: no-repeat;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
}
    .subscrimg:before{
      content: " ";
    position: absolute;
    background: rgba(0, 0, 0, 0.2);
    /* height: 59vh; */
    top: -6px;
    bottom: 0%;
    right: 0%;
    left: 0%;
    }
    .chocetitle {
    text-align: center;
    font-family: 'Hunterswood';
    font-weight: 300;
    padding-top: 3%;
    font-size: 60px;
    padding-bottom: 2%;
}
    .subkatitle{
      text-align: center;
    color: #fff;
    font-family: 'Hunterswood';
    font-style: normal;
    font-weight: 400;
    font-size: 65px;
    line-height: 66px;
    letter-spacing: 0px;
    margin-bottom: 0px;
      }
    .prodtitle {
    font-size: 38px;
    font-family: 'Hunterswood';
    font-weight: 300;
      margin-bottom: 0px;
}
    .customslider{
          background-color: #F48F89;
    color: #fff;
    }
  </style>
{% schema %}
  {
  "name": "SubscribeProduct",

  "settings": [
  {
    "id" : "komtitle",
    "type" : "text",
    "label" : "Heading"
  }
  ],
  "blocks":[
  {
  "type" : "block",
  "name" : "block",
  "settings" : [
  {
               "type": "product",
               "id": "product",
               "label": "Product"
            }
  ]
  }

  ],
  "presets" : [
  {
    "name" : "SubscribeProduct",
    "category" : "SubscribeProduct"

    }
  ]
  }
  {% endschema %}